import palette from './palette';

const footer = {
    background: palette.complementary.dark,
    fontColor: '#fff', //default primary.contrastText
    navFontColor: '', //default primary.contrastText
    fontHoverColor: '#fff', //default primary.contrastText
    navFontHoverColor: '', //default inherit
};

export default footer;